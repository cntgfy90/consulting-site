import styled from 'styled-components';

import { FlowText } from '../../shared/FlowText';

const BlockText = styled(FlowText)`
  font-family: 'Montserrat-ExtraBold';
  font-weight: 900;
  font-size: 16px;
  color: #fff;
  text-align: center;
  margin: 20px 0;
`;

export default BlockText;
